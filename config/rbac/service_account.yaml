apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-cleanup-operator
  namespace: pod-cleanup-operator-system
  labels:
    app.kubernetes.io/name: pod-cleanup-operator
    app.kubernetes.io/component: controller
